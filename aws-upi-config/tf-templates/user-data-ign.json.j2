{
  "ignition": {
    "config": {% raw %}{{% endraw %}{% if item.IGNITION_URL is defined %}

      "merge": [
        {
          "source": "{{item.IGNITION_URL}}",
          "verification": {}
        }
      ]
    {% endif %}},
    "security": {
      "tls": {}
    },
    "timeouts": {},
    "version": "{{ ignition_version | d('3.2.0', true) }}"
  },
  "networkd": {},
  "passwd": {% raw %}{{% endraw %}{% if item.USER_AUTH_KEY is defined %}

    "users": [
      {
        "name": "core",
        "sshAuthorizedKeys": [
          "{{item.USER_AUTH_KEY}}"
        ]
      }
    ]
  {% endif %}},
  "storage": {},
  "systemd": {}
}
